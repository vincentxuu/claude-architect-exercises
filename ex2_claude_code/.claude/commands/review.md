Review the current file or selection against our team checklist:

1. **Types:** All function parameters and return values have type hints
2. **Error handling:** No bare `except:`, all errors are specific types
3. **Tests:** Are there corresponding tests? Do they follow AAA pattern?
4. **Naming:** Functions are verbs, classes are nouns, variables are descriptive
5. **Security:** No hardcoded secrets, SQL uses parameterized queries

For each issue found, provide: location, problem, suggested fix.
